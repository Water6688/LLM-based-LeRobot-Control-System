<launch>
    <!-- 路径定义（let 写法保留，你已确认可用） -->
    <let name="urdf_path" value="$(find-pkg-share my_robot_description)/urdf/my_robot.urdf.xacro" />
    <let name="config_path" value="$(find-pkg-share my_robot_bringup)/config/my_robot_controllers.yaml" />
    <let name="rviz_config_path" value="$(find-pkg-share my_robot_description)/rviz/urdf_config.rviz" />

    <!-- ======================== 核心功能节点 ======================== -->
    <!-- robot_state_publisher（与Python版完全一致） -->
    <node pkg="robot_state_publisher"
          exec="robot_state_publisher"
          name="robot_state_publisher"
          output="screen">
        <param name="robot_description" value="$(command 'xacro $(var urdf_path)')" />
    </node>

    <!-- ros2_control 核心节点（与Python版完全一致） -->
    <node pkg="controller_manager"
          exec="ros2_control_node"
          name="controller_manager"
          output="screen">
        <!-- 加载控制器配置（关键！） -->
        <param from="$(var config_path)" />
        <param name="robot_description" value="$(command 'xacro $(var urdf_path)')" />
    </node>

    <!-- 启动所有控制器（与Python版完全一致） -->
    <node pkg="controller_manager" exec="spawner" args="joint_state_broadcaster" output="screen"/>
    <node pkg="controller_manager" exec="spawner" args="arm_controller" output="screen"/>
    <node pkg="controller_manager" exec="spawner" args="gripper_controller" output="screen"/>

    <!-- RVIZ2（与Python版完全一致） -->
    <node pkg="rviz2"
          exec="rviz2"
          name="rviz2"
          output="screen"
          args="-d $(var rviz_config_path)" />

</launch>